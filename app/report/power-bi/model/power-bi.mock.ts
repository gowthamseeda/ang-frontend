import { PowerBiDetail } from './power-bi.model';

export const powerBiDetailMock: PowerBiDetail = {
  embeddedToken: 'TOKEN',
  groupId: 'GROUP',
  reportId: 'REPORT'
};

export const countryMock = ['US'];
